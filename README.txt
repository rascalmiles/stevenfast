Notes
open a terminal (command + space)

if running a project on a new computer, install node, install git, and run 

sudo npm install -g http-server


everytime we run 
navigate to this project by typing 

then run it with 
http-server -c-1

then point your browser at http://localhost:8080/
right click - inspect in browser to see the command window

while working on a project:
open a terminal, navigate to your project

do this if you added new files:
git add .

everytime you want to make changes to github:
git commit -am'commit message goes here'
git push